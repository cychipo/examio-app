import SwiftUI

struct StudyView: View {
    var body: some View {
        PlaceholderScreen(
            title: "Học tập",
            description: "Quiz, flashcard, tài liệu và tiến độ luyện tập sẽ được gom tại khu vực này.",
            note: "Danh sách dài cần pagination hoặc lazy loading khi nối API thật."
        )
    }
}
